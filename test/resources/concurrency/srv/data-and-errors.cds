@graphql
service DataAndErrorsService {
  entity A {
    key id        : UUID;
        timestamp : Timestamp;
  }

  entity B {
    key id        : UUID;
        timestamp : Timestamp;
  }

  entity C {
    key id        : UUID;
        timestamp : Timestamp;
  }
}
